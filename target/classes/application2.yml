spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/iacademyDB?useSSL=false&serverTimezone=UTC
    username: iacademy
    password: icademy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
